[project]
name = "logjam"
version = "0.1.0"
description = "A configurable tool for filtering text files."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package=true

[dependency-groups]
dev = [
    "pytest>=8.4.1",
]

[project.scripts]
logjam = "logjam.cli.main:main"
